<p>Welcome <%= @email %>!</p>

<p>You can confirm your account email through the link below:</p>

<p><%#= link_to 'Confirm my account', user_confirmation_url(@resource, confirmation_token: @token) %></p>
 <div class="bg_colr_content" style="">
                      <%= link_to 'Verify my email address', confirmation_url(@resource, confirmation_token: @token), class:"default_btn"%>
                    </div>
